version: "3.9"

services:
  rewordify-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rewordify-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
